To Setup:
1. In PS C:\Users\Howard Wai\Desktop\University Work\COM5409\Assignment\task-manager\backend>
run npm init -y this initialises the backend.
2. In the same directory run npm install express cors dotenv @prisma/client.
3. Also in the same directory run npm install -D prisma these two commands installs the required dependencies.
4. For the rest of the files I created them manually instead of using the commands.
5. npm install @prisma/client
6. npm install -D prisma
7. npx prisma migrate dev --name init
8. git remote add origin https://github.com/hw10mpo/task-manager.git
